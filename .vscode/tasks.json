{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "deployto2040",
            "type": "shell",
            "command": "${workspaceFolder}/firmware/scripts/deploy.bat ${fileDirname}",
            "problemMatcher": []
        }
    ]
}